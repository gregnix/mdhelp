#!/bin/bash
# build.sh -- VERALTET / DEPRECATED
#
# Bitte build.tcl verwenden:
#   tclsh build.tcl
#
# build.tcl laeuft auf Linux und Windows (mit BAWT tclsh).
echo "Bitte build.tcl verwenden: tclsh build.tcl"
exit 1
